# TODO — MUC MVP 開發待辦清單

> **版本**：v1.2
> **建立日期**：2026-03-10
> **最後更新**：2026-03-22（F66 付款方式 + UI/UX 技術債 + Tabs 修復 + SendGrid 修正）
> **狀態**：🚧 開發中
>
> **當前重點**：Phase 14 — Credit 系統 + 產品優化（規劃完成，待開工）

---

## 進度總覽

| 階段 | 內容 | 狀態 |
|------|------|------|
| Phase 1 | 專案骨架 + Auth | ✅ 完成 |
| Phase 2 | License + Pair Code + Subscription | ✅ 完成 |
| Phase 3 | Payment | ✅ 完成 |
| Phase 4 | BillingProfile | ✅ 完成 |
| Phase 5 | Device Pairing | ✅ 完成 |
| Phase 6 | Audit Log + Alert 整合 | ✅ 完成 |
| Phase 7 | Entitlement + Pre-Job Check | ✅ 完成 |
| Phase 8 | Notification | ✅ 完成 |
| Phase 9 | 前台 UI（shadcn-vue） | ✅ Admin + Portal 全完成 |
| Phase 10 | 測試 | ✅ 完成 |
| Phase 11 | Agent SDK + 部署 | ✅ 完成（對方驗收 19/19 通過） |
| Phase 12 | User 自助付款 | ✅ 完成（F51-F53） |
| Phase 13 | 最小營運範疇 + 全自助訂購 | ✅ 完成（Step 54-62） |
| Phase 13.5 | F66 付款方式管理 + UI/UX 技術債 | ✅ 完成（2026-03-22） |
| Phase 14 | Credit 點數系統 + 產品優化 | ⬜ 規劃完成，待開工 |
| Phase 15 | 自動金流串接 | ⬜ 架構已預留（ADR-030），待選金流商 |
| Phase 16 | 推薦分潤 + 介紹人折抵 | ⬜ 待規劃 |
| Phase 17 | 自動更新發佈平台 | ⬜ 待規劃 |

---

## Phase 1：專案骨架 + Auth ✅

### P1-1：專案骨架

- [x] 建立 `backend/` 目錄結構（NestJS Modular Monolith）
- [x] 建立 `frontend/` 目錄結構（Vue 3 + Vite + Tailwind）
- [x] 建立 11 個模組骨架（auth / account / billing-profile / license / subscription / payment / device / entitlement / notification / alert / audit-log）
- [x] 設定 `nest-cli.json` / `tsconfig.json` / `.env.example`
- [x] 設定 `vite.config.ts` / `tailwind.config.js` / router / Pinia store
- [x] 建立 Portal Layout + Admin Layout + 所有 View 空殼
- [x] Backend 起動驗證（port 3100）✅
- [x] Frontend 起動驗證（port 5174）✅

### P1-2：Migration — accounts

- [x] 建立 `1741580000000-CreateAccounts.ts`
- [x] 執行 Migration（`accounts` 表已建立）
- [x] 確認欄位：id / email / passwordHash / name / status / role

### P1-3：Auth 模組

- [x] 安裝 `@nestjs/jwt` / `@nestjs/passport` / `passport-jwt`
- [x] 建立 `JwtStrategy`（Bearer token 驗證）
- [x] 建立 `JwtAuthGuard`
- [x] 建立 `AdminGuard`（role = ADMIN 才通過）
- [x] 建立 `@CurrentUser()` 裝飾器
- [x] 實作 `POST /api/v1/auth/login`（回傳 JWT token）
- [x] 實作 `POST /api/v1/auth/register`
- [x] 實作 `GET /api/v1/auth/me`（JWT Guard 保護）
- [x] API 測試通過（register / login / me）

---

## Phase 2：License + Pair Code + Subscription ✅

### P2-1：Migration — licenses + pair_codes ✅

- [x] 建立 `CreateLicenses` migration（1741580300000）
  - 欄位：id / account_id / serial_no / status(ACTIVE/INACTIVE/VOID) / note / created_at / updated_at
  - 索引：UQ(serial_no)，FK(account_id → accounts.id)
- [x] 建立 `CreatePairCodes` migration（1741580400000）
  - 欄位：id / account_id / license_id / code_hash / expires_at / used_at / created_by_user_id / created_at
  - 規則：code_hash 不存明文（ref STATE_RULES §6.3）
  - FK：license_id → licenses.id，account_id → accounts.id
- [x] 執行並驗證（含 up/down/up 測試）

### P2-2：License 模組 ✅

- [x] 實作 `POST /api/v1/licenses/admin`（Admin 建立序號，初始 INACTIVE）
- [x] 實作 `GET /api/v1/licenses/admin/list`（Admin 查詢全部）
- [x] 實作 `GET /api/v1/licenses/admin/:id`（Admin 查詢詳情，含 Account 摘要）
- [x] 實作 `POST /api/v1/licenses/admin/:id/activate`（INACTIVE → ACTIVE）
- [x] 實作 `POST /api/v1/licenses/admin/:id/deactivate`（ACTIVE → INACTIVE）
- [x] 實作 `POST /api/v1/licenses/admin/:id/void`（INACTIVE → VOID，不可逆）
- [x] 實作 `GET /api/v1/licenses/my`（User 查詢自己的序號）
- [x] 加上 `JwtAuthGuard` + `AdminGuard` 保護 Admin 路由
- [x] 所有狀態變更寫入 audit log
- [x] API 手動驗證通過

### P2-3：Pair Code 生成 ✅

- [x] 實作 `POST /api/v1/licenses/:id/pair-code`
  - 生成高熵隨機碼（crypto.randomBytes 16 bytes → 32 hex）
  - 計算 SHA-256 hash，存入 DB（不存明文）
  - 設定 expiresAt = now + 300 秒（OQ-005 已拍板 5 分鐘）
  - 回傳明文 + expiresAt（只回傳一次）
- [x] 驗證：同一 License 不得同時存在未過期且未使用的 PairCode（PAIR_CODE_ALREADY_ACTIVE）
- [x] 所有權檢查：License 不屬於自己 → FORBIDDEN
- [x] License 狀態檢查：非 ACTIVE → PAIRING_NOT_ALLOWED
- [x] audit_log：PAIR_CODE_CREATED
- [x] API 手動驗證通過

### P2-4：Migration — subscriptions ✅

- [x] 建立 `CreateSubscriptions` migration（1741580500000）
  - 欄位：id / license_id / status(ACTIVE/GRACE/SUSPENDED) / period_start_date(DATE) / period_end_date(DATE) / grace_end_date(DATE) / cancel_at_period_end(BOOLEAN) / last_state_calc_at(DATETIME nullable) / created_at / updated_at
  - 索引：UQ(license_id)，idx(status)，idx(period_end_date)，idx(grace_end_date)
  - FK：license_id → licenses.id
  - 時區：DATE 欄位以 Asia/Taipei 日曆日為準（ref STATE_RULES §2.1 已更新）
- [x] 執行並驗證（含 up/down/up 測試）

### P2-5：Subscription 狀態機 ✅

- [x] 安裝 dayjs（時區處理）
- [x] 實作 `activateOrRenew(licenseId, confirmedAt)`
  - confirmedAt UTC → Asia/Taipei 日期 → periodStartDate
  - periodEndDate = +1 calendar month（dayjs 處理月底邊界）
  - graceEndDate = periodEndDate + 1 day
  - 新建 → SUBSCRIPTION_CREATED / 更新 → SUBSCRIPTION_REACTIVATED
  - 決策：`last_payment_confirmed_at` 不存欄位，由呼叫方參數傳入（方案 B）
- [x] 實作 `transitionToGrace(subscriptionId)`
  - 前置條件：status = ACTIVE（否則 409 INVALID_STATE_TRANSITION）
  - 冪等：已是 GRACE → skip（不重複寫 audit log）
  - audit_log：SUBSCRIPTION_ENTERED_GRACE
  - notification：TODO（Phase 8 實作）
- [x] 實作 `transitionToSuspended(subscriptionId)`
  - 前置條件：status = GRACE（否則 409 INVALID_STATE_TRANSITION）
  - 冪等：已是 SUSPENDED → skip
  - audit_log：SUBSCRIPTION_SUSPENDED
  - notification：TODO（Phase 8 實作）
- [x] 測試：7 個 BDD 場景全部通過
  - Scenario 1：首次 activate → ACTIVE ✅
  - Scenario 2：ACTIVE → GRACE（Reconciler）✅
  - Scenario 3：GRACE → SUSPENDED（Reconciler）✅
  - Scenario 4：ACTIVE/GRACE/SUSPENDED → ACTIVE（renew）✅
  - Scenario 5：ACTIVE 不得直接 → SUSPENDED ✅
  - Scenario 6：Reconciler 重跑不重複 side effects ✅
  - Scenario 7：架構保證 DRAFT Payment 不觸發 activateOrRenew ✅

### P2-6：Subscription Reconciler ✅

- [x] `@Cron` 定時執行（預設每 5 分鐘，可透過 RECONCILER_CRON 環境變數覆寫）
- [x] 查詢 ACTIVE 且 Asia/Taipei today > periodEndDate → 批次轉 GRACE
- [x] 查詢 GRACE 且 Asia/Taipei today > graceEndDate → 批次轉 SUSPENDED
- [x] 冪等保證：Service 層判斷目標狀態已到達則 skip
- [x] 測試：重跑不產生重複 audit log ✅
- [x] 新增臨時 admin 端點 `POST /api/v1/subscriptions/admin/reconcile`（Phase 3 後可移除）
- [x] 新增臨時 admin 端點 `POST /api/v1/subscriptions/admin/activate`（Phase 3 Payment 連動後替換）

---

## Phase 3：Payment ✅

### P3-1：Migration — payments ✅

- [x] 建立 `CreatePayments` migration（1741580600000）
  - 欄位：id / license_id / status(DRAFT/CONFIRMED/VOID) / amount(INT) / currency(VARCHAR DEFAULT 'TWD') / note / confirmed_by_admin_id / confirmed_at / created_at / updated_at
  - 索引：idx(license_id)、idx(status)
  - FK：license_id → licenses.id、confirmed_by_admin_id → accounts.id
  - 測試：up/down/up 通過、ENUM 驗證、nullable 驗證
- [x] 更新 Entity：amount 改 int（最小單位整數）、加 column name mapping、precision 6

### P3-2：Payment 模組 ✅

- [x] 實作 `POST /api/v1/payments/admin`（Admin 建立草稿）
  - status = DRAFT，HTTP 201
  - audit_log：PAYMENT_DRAFT_CREATED
- [x] 實作 `POST /api/v1/payments/admin/:id/confirm`（Admin 確認收款）
  - 前置條件：status = DRAFT
  - 已 CONFIRMED → 409 PAYMENT_ALREADY_CONFIRMED（OQ-004 Option B）
  - DB 條件更新（WHERE status='DRAFT'）防並發
  - 成功 → CONFIRMED + confirmedAt + 觸發 subscriptionService.activateOrRenew()
  - audit_log：PAYMENT_CONFIRMED
  - notification：TODO（Phase 8）
- [x] 實作 `POST /api/v1/payments/admin/:id/void`（Admin 作廢草稿）
  - 前置條件：status = DRAFT
  - CONFIRMED → 409 PAYMENT_CANNOT_BE_VOIDED
  - void reason 記錄在 audit log payload
  - audit_log：PAYMENT_VOIDED
- [x] 實作 `GET /api/v1/payments/license/:licenseId`（查詢付款紀錄）
- [x] DTO 驗證：AdminCreatePaymentDto + AdminConfirmPaymentDto
- [x] 所有 admin 路由加 JwtAuthGuard + AdminGuard
- [x] 測試：8 個 BDD 場景全部通過（含並發保護、缺欄位驗證）

### P3-3：Payment → Subscription 連動 ✅

- [x] F12 驗證：首次 confirm → 自動建立 Subscription（ACTIVE + 正確週期）
- [x] F12 驗證：ACTIVE/GRACE/SUSPENDED 任意狀態 confirm → 恢復 ACTIVE
- [x] F12 驗證：月底邊界（1/31 → period_end 2/28 → grace_end 3/1）
- [x] F12 修正：adminConfirm 用 DataSource.transaction() 包裝（Payment + Subscription + audit log 原子操作）
- [x] F12 驗證：重複 confirm 不重複延長、CONFIRMED 不可 void

---

## Phase 4：BillingProfile ✅

### P4-1：Migration — billing_profiles ✅

- [x] 建立 `CreateBillingProfiles` migration（1741580700000）
  - 欄位：id / account_id / company_name / tax_id / invoice_title / invoice_email / address / created_at / updated_at
  - 約束：UQ(account_id)、FK(account_id → accounts.id)
  - 變更：contactEmail → invoice_email、新增 invoice_title（BDD 驅動）
- [x] 更新 Entity：加 column name mapping、明確 type: 'varchar'、nullable 型別
- [x] 測試：8 個場景通過（含 FK、UNIQUE、rollback）

### P4-2：BillingProfile 模組 ✅

- [x] 實作 `GET /api/v1/billing-profiles/my`（User 查詢自己）
- [x] 實作 `POST /api/v1/billing-profiles`（建立，重複 → 409）
- [x] 實作 `PUT /api/v1/billing-profiles/:id`（更新，ownership check → 403）
- [x] 加上 `JwtAuthGuard` 保護
- [x] DTO 驗證：company_name 必填、tax_id 8 位數字、invoice_email 有效 email
- [x] audit_log：BILLING_PROFILE_UPDATED（create/update 皆寫入）
- [x] 修正 ValidationPipe errorHttpStatusCode: 422（與 BDD 定義一致）
- [x] 測試：F14 全部 11 個 BDD 場景通過

---

## Phase 5：Device Pairing ✅

### P5-1：Migration — devices + device_tokens ✅

- [x] 建立 `CreateDevices` migration（1741580800000）
  - 欄位：id / license_id / account_id / status(ENUM: ACTIVE/REVOKED/REPLACED) / fingerprint / revoked_at / created_at / updated_at
  - FK：license_id → licenses.id、account_id → accounts.id
  - 索引：idx(license_id, status)、idx(account_id)
  - 修正 collation：utf8mb4_unicode_ci（與現有表一致）
- [x] 建立 `CreateDeviceTokens` migration（1741580900000）
  - 欄位：id / device_id / token_hash / revoked_at / created_at（無 updated_at，語意正確）
  - FK：device_id → devices.id（CASCADE）
  - UQ：token_hash
  - 規則：不存明文（ref STATE_RULES §7.2）
- [x] 測試：D15 全部 7 個 BDD 場景通過（含 rollback + re-run）

### P5-2：Agent Pair（核心）✅

- [x] 實作 `POST /api/v1/devices/agent/pair`
  - 驗證 timestamp（±5 分鐘容許窗口）
  - 驗證 nonce（in-memory replay protection + TTL cleanup）
  - 驗證 PairCode：SHA-256 hash 比對、未過期、未使用
  - 驗證 License：同一 License 無現有 ACTIVE Device
  - DataSource.transaction() 原子操作：建立 Device + DeviceToken + 標記 PairCode used
  - DeviceToken：randomBytes(32) → hex，DB 只存 SHA-256 hash，明文只回傳一次
  - audit_log：DEVICE_PAIRED + PAIR_CODE_USED
- [x] 錯誤碼全部實作：
  - PAIR_CODE_EXPIRED / PAIR_CODE_USED / PAIR_CODE_INVALID
  - LICENSE_ALREADY_BOUND / NONCE_REPLAY / VALIDATION_ERROR
- [x] DTO 驗證：AgentPairDto（pairCode / fingerprint / agentVersion / timestamp / nonce）
- [x] 測試：F16 全部 10 個 BDD 場景通過

### P5-3：Device Revoke ✅

- [x] 實作 `POST /api/v1/devices/:id/revoke`（User 自助，ref ADR-018, OQ-008）
  - Ownership check：device.accountId === actorId
  - Device status → REVOKED + revokedAt
  - DeviceToken revokedAt = now（批次更新）
  - audit_log：DEVICE_REVOKED（含 before/after status + reason）
  - 錯誤碼：DEVICE_NOT_FOUND / DEVICE_ALREADY_REVOKED / FORBIDDEN
  - Request body：optional `reason` 欄位（RevokeDeviceDto）
  - Response body：`{ deviceId, status: 'REVOKED' }`
- [x] 實作 `GET /api/v1/devices/my`（User 查詢自己的設備）
- [x] 測試：17_device_revoke 6/6 可驗證場景通過（3 個 Entitlement 場景延後 Phase 7）
- [x] 實作 Admin 強制 revoke（Step 28 Admin UI 設備管理已完成）

---

## Phase 6：Audit Log + Alert 整合 ✅

### P6-1：Migration — alerts + ALTER audit_logs ✅

- [x] 建立 `CreateAlerts` migration（1741581000000）
  - 欄位：id / level(varchar) / type(varchar) / account_id(nullable FK) / license_id(nullable FK) / status(varchar, default OPEN) / message(text) / meta_json(json) / created_at / updated_at
  - FK：account_id → accounts(ON DELETE SET NULL)、license_id → licenses(ON DELETE SET NULL)
  - 索引：level / type / account_id / license_id / status
- [x] 建立 `AlterAuditLogs` migration（1741581100000）
  - 改名：event→action、camelCase→snake_case（actor_id / target_id / target_type / created_at）
  - 新增：actor_type(varchar) / ip / user_agent / request_id / before_json / after_json
  - 移除：payload 欄位（拆分為 before_json + after_json）
  - 索引：actor_type / actor_id / action / target_type / target_id / request_id / created_at (7 個)
  - 無 updated_at（append-only）
- [x] Entity 更新：Alert（varchar 型別 + account_id/license_id/message/metaJson）+ AuditLog（新 schema）
- [x] AuditLogService：新增 `log()` 方法 + 保留 `write()` 向下相容
- [x] AlertService：更新 `create()` 簽章（level / type / message / meta）
- [x] 測試：D18 全部 9 個 BDD 場景通過（含 rollback + re-run）

### P6-2：Audit Log 全面整合 ✅

- [x] 新增 `ActorType` enum（SYSTEM / USER / PLATFORM_ADMIN / AGENT）
- [x] `write()` 方法新增 actorType 參數，精確區分 admin/user/system
- [x] 所有 target_type 統一為全大寫（PAYMENT / DEVICE / PAIR_CODE / SUBSCRIPTION / ALERT / LICENSE / ACCOUNT / BILLING_PROFILE）
- [x] Admin 操作傳 `ActorType.PLATFORM_ADMIN`（payment / license / alert）
- [x] AlertService ack/resolve 注入 AuditLogService，寫入 ALERT_ACKED / ALERT_RESOLVED
- [x] AlertController 加 JwtAuthGuard + AdminGuard + @CurrentUser
- [x] AlertModule imports AuditLogModule
- [x] 確認 16 個 audit action 全部正確覆蓋：
  - ACCOUNT_LOGGED_IN / SUBSCRIPTION_CREATED / ENTERED_GRACE / SUSPENDED / REACTIVATED
  - PAYMENT_DRAFT_CREATED / CONFIRMED / VOIDED
  - DEVICE_PAIRED / REVOKED / LICENSE_CREATED / ACTIVATED
  - PAIR_CODE_CREATED / USED / ALERT_ACKED / ALERT_RESOLVED
- [x] 測試：F19 全部 11 個 BDD 場景通過

### P6-3：Alert 事件映射整合 ✅

- [x] 新增 `ALERT_EVENT_MAP` 常數（9 事件→等級映射，ref ADR-019）
- [x] 新增 `createByEvent()` 方法（統一入口，不支援事件回傳 null）
- [x] 整合 Alert 至各模組（4/9 事件，其餘 5 個待 Phase 7 Entitlement）：
  - PAYMENT_CONFIRMED → INFO Alert（PaymentService.adminConfirm）✅
  - DEVICE_REGISTERED → INFO Alert（DeviceService.agentPair）✅
  - SUBSCRIPTION_GRACE → WARN Alert（SubscriptionService.transitionToGrace）✅
  - SUBSCRIPTION_SUSPENDED → CRITICAL Alert（SubscriptionService.transitionToSuspended）✅
- [x] 模組依賴更新：PaymentModule / DeviceModule / SubscriptionModule 加入 AlertModule
- [x] 測試：F20 9/9 可測場景通過（5 個 Entitlement 相關延後 Phase 7）
- [x] Phase 7 補齊（3/5 已完成）：
  - FINGERPRINT_CHANGED → WARN ✅（Entitlement 驗證時偵測）
  - TOKEN_REPLAY_SUSPECTED → CRITICAL ✅（Entitlement nonce 驗證）
  - SAME_TOKEN_MULTI_DEVICE → CRITICAL ✅（Entitlement 偵測）
  - SIGNATURE_VERIFICATION_FAILED → Agent 端行為，非 Server 實作
  - FREQUENT_FAILED_PAIRING → WARN（需計數器，延後）

---

## Phase 7：Entitlement + Pre-Job Check ✅

### P7-1：Ed25519 Key 設定 ✅

- [x] 生成 Ed25519 Key Pair（node crypto.generateKeyPairSync）
- [x] 填入 `.env`：ED25519_PRIVATE_KEY / ED25519_PUBLIC_KEY（hex-encoded DER）
- [x] Agent 端已內建同一把公鑰（muc_agent.py v0.1.0）

### P7-2：Entitlement 模組 ✅

- [x] 實作 `POST /api/v1/agent/entitlement`
  - 驗證 Bearer device_token（SHA-256 hash 比對 DeviceToken 表）
  - 驗證 Device status = ACTIVE（REVOKED/REPLACED → TOKEN_REVOKED）
  - 驗證 device_id 一致（不一致 → SAME_TOKEN_MULTI_DEVICE Alert + DEVICE_MISMATCH）
  - 驗證 fingerprint strict policy（ref ADR-012 → DEVICE_MISMATCH + FINGERPRINT_CHANGED Alert）
  - 驗證 timestamp ±5 分鐘
  - 驗證 nonce 防重放（in-memory Map + 10min TTL → NONCE_REPLAY + TOKEN_REPLAY_SUSPECTED Alert）
  - 驗證 Subscription status = ACTIVE or GRACE（SUSPENDED → SUBSCRIPTION_SUSPENDED）
  - 回應：signed entitlement（payload + key_id + issued_at + expires_at + signature）
  - Ed25519 私鑰簽章（Node.js crypto.sign）
  - TTL = 5 分鐘（ref OQ-006 暫定）
  - key_id = "default"（ref ADR-020 預留欄位）
- [x] 錯誤碼全覆蓋：TOKEN_INVALID / TOKEN_REVOKED / DEVICE_MISMATCH / NONCE_REPLAY / SUBSCRIPTION_SUSPENDED
- [x] Alert 整合：FINGERPRINT_CHANGED(WARN) / TOKEN_REPLAY_SUSPECTED(CRITICAL) / SAME_TOKEN_MULTI_DEVICE(CRITICAL)
- [x] Audit log：ENTITLEMENT_DENIED（安全事件）
- [x] BDD F21 測試：19/19 場景通過

### P7-3：Pre-Job Check ✅

- [x] 實作 `POST /api/v1/agent/check-before-job`
  - 驗證 Bearer device_token + device status + device_id + timestamp + nonce + subscription
  - 回應：`{ allow_new_jobs: boolean, checked_at: string }`
  - 成功 → audit log PRE_JOB_CHECK_PASSED
  - 失敗 → audit log PRE_JOB_CHECK_FAILED
  - nonce replay → TOKEN_REPLAY_SUSPECTED Alert
- [x] BDD 場景通過（含 SUSPENDED denied）
- [x] 離線 15 分鐘政策 — Agent SDK 已實作（muc_agent.py 離線政策判斷）

---

## Phase 8：Notification ✅ 完成（2026-03-11）

### P8-1：SendGrid 整合 ✅

- [x] `.env` 已有 SENDGRID_API_KEY / EMAIL_FROM
- [x] Migration D23：`notifications` 表（type/status/idempotency_key/recipient_email/sent_at/provider_message_id）
- [x] Notification Entity + NotificationType (PAYMENT_CONFIRMED / SUBSCRIPTION_SUSPENDED)
- [x] NotificationService 重寫：createJob() + processJob() + resolveRecipientEmail()
- [x] Bull queue 整合（@nestjs/bull + BullModule.forRoot + notification queue）
- [x] NotificationProcessor（Bull worker，send-email job，attempts=3，exponential backoff）
- [x] Email 模板 2 類（ADR-021 混合策略，GRACE 只走 Agent）：
  - 收款確認（PAYMENT_CONFIRMED）
  - 已停權（SUBSCRIPTION_SUSPENDED）
- [x] 冪等：idempotency_key 唯一約束，重複 createJob 自動跳過
- [x] 無收件者 email → status=FAILED, errorMessage=RECIPIENT_EMAIL_MISSING
- [x] 收件者解析：BillingProfile.invoiceEmail > Account.email
- [x] AuditEvent.NOTIFICATION_SENT 新增

### P8-2：Notification 整合至各模組 ✅

- [x] PaymentService.adminConfirm → PAYMENT_CONFIRMED 通知（transaction 外、try/catch 非阻塞）
- [x] SubscriptionService.transitionToSuspended → SUBSCRIPTION_SUSPENDED 通知
- [x] SubscriptionService.transitionToGrace → 標註 Agent-side only（ADR-021）
- [x] PaymentModule + SubscriptionModule 加入 NotificationModule import
- [x] BDD F23 全 15 場景通過

---

## Phase 9：前台 UI ✅

> **UI 元件庫**：shadcn-vue（ADR-022）
> **頁面規格來源**：MVP 網站系統架構 v1.0（§4 MVP 範圍 + §15 頁面清單）

### P9-0：shadcn-vue 初始化 ✅

- [x] 安裝 shadcn-vue + Radix Vue 依賴
- [x] 初始化 `components.json` 設定
- [x] 安裝常用基礎元件（Button / Table / Dialog / Badge / Form / Input / Tabs / Card / DropdownMenu）
- [x] 建立共用 Layout（AdminLayout / PortalLayout）
- [x] 設定 i18n 基礎架構（所有文字用 i18n key）

### P9-1：Admin 後台（7 頁）✅

#### P9-1-1：收款管理頁 `/admin/payments` ✅
- [x] DRAFT 列表（payment_id / license_id / amount / currency / created_at / note）
- [x] confirm / void 操作 + 二次確認彈窗
- [x] 防重複送出（loading 狀態）
- [x] 錯誤處理（conflict / 權限錯誤）
- [x] 操作後自動刷新列表

#### P9-1-2：序號管理頁 `/admin/licenses` ✅
- [x] License 列表（serial_no / account / status / created_at）
- [x] 建立序號
- [x] 查看序號綁定設備
- [x] 啟用 / 停用 / 作廢操作

#### P9-1-3：帳號管理頁 `/admin/accounts` ✅
- [x] 帳號列表（email / role / status 篩選 + 分頁）
- [x] 查看帳號狀態 + 底下序號/設備
- [x] 建立帳號 Dialog
- [x] 停用 / 啟用操作 + 二次確認

#### P9-1-4：設備管理頁 `/admin/devices` ✅
- [x] 設備列表（device_name / license / status / last_seen_at）
- [x] Admin 強制 revoke

#### P9-1-5：告警中心頁 `/admin/alerts` ✅
- [x] Alert 列表（level badge / type / message / status / created_at）
- [x] 篩選：level / status
- [x] ack / resolve 操作

#### P9-1-6：稽核紀錄頁 `/admin/audit-logs` ✅
- [x] 唯讀列表（action / actor / target / created_at）
- [x] 篩選 action 類型
- [x] 分頁

#### P9-1-7：Dashboard `/admin/dashboard` ✅
- [x] 總覽統計卡片（帳號數 / License 數 / ACTIVE 數 / 待確認收款數）
- [x] 最近告警摘要
- [x] 6 KPI 卡片（pendingPayments/activeSubscriptions/graceSubscriptions/suspendedSubscriptions/openAlerts/todayNewDevices）
- [x] KPI 卡片可點擊跳轉對應管理頁
- [x] 3 待辦列表（待確認收款 / 即將到期 / GRACE 告警）
- [x] 近期事件表（audit log 最新 10 筆）
- [x] Skeleton loading 狀態

### P9-1-8：RBAC 角色權限管理（獨立步驟 32 — F32）✅

> 導入完整 RBAC 系統，取代硬編碼 USER/ADMIN（ref ADR-023）

**Migration：** ✅
- [x] 建立 `roles` 表（id / role_code / role_name / status / note / is_system / created_at / updated_at）
- [x] 建立 `permissions` 表（id / permission_key / resource / action / description / created_at）
- [x] 建立 `role_permissions` 關聯表（role_id / permission_id）
- [x] ALTER `accounts`：新增 role_id FK → roles.id，遷移 role ENUM 資料
- [x] Seed：系統預設 3 角色（SUPER_ADMIN / ADMIN / USER，is_system=true）
- [x] Seed：22 個 Permission（account.read/create/update/delete, license.read/write, payment.read/create/confirm/void, device.read/revoke, alert.read/manage, audit.read, role.read/create/update/delete/assign, permission.read, dashboard.read）

**後端 API：** ✅
- [x] `GET /admin/roles` — 角色列表（需 role.read）
- [x] `POST /admin/roles` — 建立角色（需 role.create，role_code 唯一）
- [x] `PUT /admin/roles/:id` — 更新角色名稱/描述（需 role.update，系統角色 role_code 不可改）
- [x] `POST /admin/roles/:id/disable` — 停用角色（需 role.delete，軟刪除 → DISABLED）
- [x] `GET /admin/roles/:id/permissions` — 查詢角色權限（需 role.read）
- [x] `PUT /admin/roles/:id/permissions` — 設定角色權限（需 role.update，全量覆蓋）
- [x] `GET /admin/permissions` — 列出所有可用權限（需 permission.read）
- [x] `POST /admin/accounts/:id/role` — 指派角色給帳號（需 role.assign）
- [x] audit log：ROLE_CREATED / ROLE_UPDATED / ROLE_DISABLED / ROLE_PERMISSIONS_UPDATED / ACCOUNT_ROLE_ASSIGNED / ACCOUNT_ROLE_CHANGED
- [x] 安全防護：SYSTEM_ROLE_IMMUTABLE / ROLE_IN_USE / LAST_PRIVILEGED_ROLE_PROTECTED / SYSTEM_ROLE_PERMISSION_PROTECTED / LAST_PRIVILEGED_ADMIN_PROTECTED

**Guard 重構：** ✅
- [x] 建立 PermissionGuard + @RequirePermission() 裝飾器
- [x] 重構所有 Admin Controller（9 個）：AdminGuard → @RequirePermission('resource.action')
- [x] SUPER_ADMIN 自動通過所有權限檢查
- [x] JWT payload 調整：加入 roleId

**UI — 角色管理頁 `/admin/roles`：** ✅
- [x] 角色列表（role_code / role_name / status / member_count / updated_at / isSystem badge）
- [x] 建立角色 Dialog（role_code + role_name + note）
- [x] 編輯角色 Dialog（role_name + note，系統角色 role_code 鎖定）
- [x] 停用角色 + 二次確認（非系統角色、無帳號綁定才可）
- [x] 權限指派 UI（checkbox 矩陣，按 resource 分組）
- [x] 路由：新增 `/admin/roles` 至 router + AdminLayout sidebar
- [x] 角色指派帳號入口（從角色頁進入）

**BDD：** F32_admin_role_permission_management.feature（24 場景）

---

### P9-1-9：Admin 帳號管理功能（獨立步驟 33 — F33）

> 此功能獨立於現有 Admin UI 頁面（F25-F31），包含後端 API 新增 + UI 整合至帳號管理頁。

**後端 API：** ✅ 完成
- [x] `GET /accounts/admin/list` — 帳號列表搜尋（含 email / role / status 篩選 + 分頁）
- [x] `POST /admin/accounts/:id/role` — 指派角色（role_id，含 SELF_ROLE_DOWNGRADE_FORBIDDEN）
- [x] `POST /accounts/admin/:id/disable` — 停用帳號（LAST_ACTIVE_ADMIN + ALREADY_DISABLED 保護）
- [x] `POST /accounts/admin/:id/enable` — 啟用帳號（ALREADY_ACTIVE 保護）
- [x] `POST /accounts/admin/create` — 建立帳號並指定角色（INVALID_ROLE for SUPER_ADMIN）
- [x] audit log：ACCOUNT_CREATED / ACCOUNT_DISABLED / ACCOUNT_ENABLED / ACCOUNT_DISABLE_REJECTED / ACCOUNT_PERMISSIONS_CHANGED / ADMIN_FORBIDDEN_OPERATION
- [x] 登入檢查：DISABLED 帳號回傳 ACCOUNT_DISABLED（已有）
- [x] BDD 14/15 場景通過（permission_codes 更新為 F32 角色指派取代）

**UI 整合（加入現有帳號管理頁）：** ✅ 完成
- [x] 帳號列表增加 role_code 欄位顯示（Badge）
- [x] 列表改為 admin/list API（含 email/role/status 篩選 + 分頁）
- [x] 停用 / 啟用按鈕 + 二次確認 Dialog
- [x] 建立帳號表單 Dialog（email / password / name / role 選擇）
- [x] 錯誤處理：EMAIL_ALREADY_EXISTS / INVALID_ROLE / LAST_ACTIVE_ADMIN / ALREADY_DISABLED / ALREADY_ACTIVE
- [x] i18n 新增 20+ 條翻譯
- [x] Build 通過

**商業規則：** BR-AC1~AC6（ref DOMAIN_MODEL §6.5）
**狀態規則：** Account ACTIVE ↔ DISABLED（ref STATE_RULES §3）
**BDD：** F33_admin_account_management.feature

### P9-1-10：Admin UI 增強與重構（2026-03-12）✅

> 對照 ADMIN_UI_SPEC.md 進行 13 項差距修補 + 程式碼重構

**共用元件抽取：** ✅
- [x] `usePagination` composable（5 個 list view 共用）
- [x] `PaginationControls.vue`（共用分頁 UI）
- [x] `ConfirmActionDialog.vue`（共用確認彈窗）
- [x] `TableSkeleton.vue`（共用表格骨架屏）
- [x] `Skeleton.vue`（shadcn-vue 基礎骨架元件）

**Breadcrumb 導覽：** ✅
- [x] AdminLayout 加入 breadcrumb（讀取 route.meta.breadcrumb）
- [x] 所有 admin 路由加 meta.breadcrumb + 子頁加 meta.parent

**列表增強：** ✅
- [x] License 列表：status / subscriptionStatus / search 篩選 + periodEndDate / graceEndDate / accountEmail 欄位
- [x] Payment 列表：新增 adminFindAll API（status 篩選）+ accountEmail / serialNo / status 欄位
- [x] Account 列表：licenseCount / activeDeviceCount / lastPaymentStatus 欄位
- [x] Device 列表：status / platform 篩選下拉

**詳情 Dialog：** ✅
- [x] Alert 詳情：metaJson JSON 格式 + 跨連結到帳號/序號頁
- [x] Audit Log 詳情：IP / userAgent / requestId / beforeJson / afterJson + requestId 複製

**重構：** ✅
- [x] LicensesView 684→453 行（拆出 LicenseDetailPanel.vue 200 行）
- [x] 所有 list view 改用 parseResponse 防禦性處理 API 回應

**後端 API 修正：** ✅
- [x] license.service adminFindAll：改用 raw SQL 批次查詢（修復 TypeORM getRawAndEntities 500 錯誤）
- [x] payment.service adminFindAll：同上修正 + 新增 GET /payments/admin/list 路由
- [x] dashboard.service：6 KPI 查詢 + 4 待辦/事件查詢（Asia/Taipei timezone）
- [x] alert.service：findOne(id) + GET /admin/alerts/:id
- [x] account.service adminList：批次載入 licenseCount / activeDeviceCount / lastPaymentStatus
- [x] device.service/controller：status + platform 篩選參數

---

### P9-2：User Portal（8 頁）✅ 完成（2026-03-14）

> **規格來源**：`docs/ui/PORTAL_UI_SPEC.md`
> **範圍**：已登入區 6 頁 + 登入/註冊（含登入頁分離）

#### P9-2-1：登入頁（分離式）✅
- [x] `/portal/login` — 會員登入（左右分欄、品牌 panel + 表單）
- [x] `/admin/login` — 管理者登入（深色品牌 panel + Shield icon + 非 admin 擋回）
- [x] `/login` → redirect 至 `/portal/login`（向下相容）
- [x] Navigation guard 依目標區域導向對應登入頁
- [x] Admin 登出 → `/admin/login`
- [x] i18n 完整翻譯

#### P9-2-2：註冊頁 `/portal/register` ✅
- [x] 左右分欄（品牌 panel + 註冊表單）
- [x] 密碼強度即時指示器（弱/中/強）
- [x] 成功後 CheckCircle icon + 導向登入
- [x] Input icon（Mail / Lock / User）

#### P9-2-3：我的首頁 `/portal` ✅
- [x] 狀態摘要卡 + 操作捷徑 + 重要提醒 + 最近活動

#### P9-2-4：我的訂閱 `/portal/licenses` ✅
- [x] 訂閱狀態卡 + 序號資訊 + Pair Code 生成 + 倒數計時

#### P9-2-5：我的設備 `/portal/devices` ✅（14/14 BDD）
- [x] 設備列表 + 詳情面板 + 停用確認 + SUSPENDED 警示

#### P9-2-6：帳單/付款 `/portal/billing` ✅（18/18 BDD）
- [x] 付款提醒 + 付款紀錄 + 付款說明 + BillingProfile 表單

#### P9-2-7：通知中心 `/portal/notifications` ✅（15/15 BDD）
- [x] 通知列表 + 篩選 + 標記已讀 + 跳轉 + Empty State

#### P9-2-8：帳號設定 `/portal/settings` ✅（18/18 BDD）
- [x] 基本資料 / 帳單資料 / 修改密碼 / 通知偏好（4 區獨立儲存）

### P9-3：未登入前台（P2 延後）

> 行銷展示性質，不涉及後端邏輯，可獨立於 MVP 核心閉環之外。

#### P9-3-1：首頁（Landing Page）
- [ ] Hero 區（主標題 + 副標 + CTA）
- [ ] 核心價值卡片（3-4 個）
- [ ] 使用情境（個人/小型團隊/中小企業）
- [ ] 3 步驟操作流程
- [ ] FAQ 區
- [ ] 頁尾（聯絡/條款/隱私）

#### P9-3-2：方案/價格頁
- [ ] 方案卡片（名稱/月費/功能/適合對象/CTA）
- [ ] 方案比較表
- [ ] 推薦方案標示

---

## Phase 10：測試 ✅ 完成（2026-03-14）

> **E2E 測試框架**：Jest + Supertest + 真實 MySQL 測試資料庫（museon_platform_test）
> **執行方式**：`npm run test:e2e`（--runInBand 串行執行）
> **結果**：4 Test Suites / 49 Tests 全部通過（~9 秒）

### P10-1：Subscription 狀態機測試（Step 41）✅ 12/12 通過

- [x] T-SUB-001：建立 Subscription（首次 confirm）
- [x] T-SUB-002：ACTIVE → GRACE（period_end 過後）
- [x] T-SUB-003：GRACE → SUSPENDED（grace_end 過後）
- [x] T-SUB-004：任意狀態 → ACTIVE（re-confirm）
- [x] Reconciler 冪等測試（重跑不重複 side effects）
- [x] 邊界條件：月底日期、audit log 驗證、side effects 驗證

### P10-2：Pair Code 驗證測試（Step 42）✅ 12/12 通過

- [x] 正常配對（完整流程）
- [x] 過期 PairCode → PAIR_CODE_EXPIRED
- [x] 已使用 PairCode → PAIR_CODE_USED
- [x] License 已有 ACTIVE Device → LICENSE_ALREADY_BOUND
- [x] fingerprint 回傳 + Token 一次性回傳
- [x] Pair Code 不可重複使用
- [x] 無效 Pair Code → PAIR_CODE_INVALID
- [x] audit log 驗證（DEVICE_PAIRED / PAIR_CODE_USED）

### P10-3：Entitlement 驗章測試（Step 43）✅ 9/9 通過

- [x] 正常 Entitlement（Ed25519 簽章驗證 ✅）
- [x] expires_at 正確（約 5 分鐘後）
- [x] SUSPENDED Subscription → allowNewJobs = false
- [x] REVOKED Device → TOKEN_REVOKED
- [x] fingerprint mismatch → DEVICE_MISMATCH（ref ADR-012）
- [x] 竄改 payload → 簽章驗證失敗（client-side）
- [x] 缺少簽章 → 拒絕（client-side）

### P10-4：離線限制測試（Step 44）✅ 16/16 通過

- [x] Server 回傳 issued_at / expires_at 時間戳
- [x] check-before-job 回傳 checked_at + allow_new_jobs
- [x] 14 分鐘內 → allowed = true
- [x] 15 分鐘（邊界）→ allowed = true
- [x] 16 分鐘 → allowed = false
- [x] 邊界條件表（0/1/14/15/16/60 分鐘）
- [x] 執行中任務不被強制終止（agent-side 邏輯模擬）
- [x] 快取 entitlement 不能繞過離線限制
- [x] PRE_JOB_CHECK_PASSED audit log 驗證

---

## Phase 11：Agent SDK ✅

> **目標**：交付 Python SDK 給 Agent 開發者，完成配對 → 授權輪詢 → 接單前檢查的完整閉環
> **技術棧**：Python 3.10+ / httpx / cryptography
> **目錄**：`agent-sdk/`

### P11-1：Agent 文件修正（Step 45）✅

- [x] AGENT_INTEGRATION_GUIDE.md v1.1
- [x] Pair 端點修正：`/api/v1/agent/pair` → `/api/v1/devices/agent/pair`
- [x] Entitlement 端點修正：`GET /api/v1/device/entitlement` → `POST /api/v1/agent/entitlement`
- [x] Headers / Body 參數對齊實際程式碼
- [x] 簽章輸入格式說明補齊

### P11-2：Agent HTTP 整合測試（Step 46）✅ 33/33 通過

- [x] `agent-sdk/test_integration.py`（Python httpx → localhost:3100）
- [x] Phase 1: Setup（admin/user login + license + payment）
- [x] Phase 2: Pairing（pair code 生成 + 配對 + 碼重用拒絕）
- [x] Phase 3: Entitlement（授權回應 + Ed25519 驗章 + 竄改偵測 + 錯誤指紋 + 無效 token）
- [x] Phase 4: Pre-Job Check（接單前檢查 + job_context）
- [x] Phase 5: Offline Policy（7 個邊界條件：0/1/14/15/16/30/60 分鐘）
- [x] Phase 6: Nonce Replay（重放攻擊偵測）
- [x] Phase 7: Revoke（設備撤銷 + token 立即失效）
- [x] 發現：Python `json.dumps` 必須用 `separators=(",",":")` 才與 JS `JSON.stringify` 一致

### P11-3：Agent SDK 模組（Step 47）✅

- [x] `agent-sdk/muc_agent.py` v0.1.0：MucAgent class
- [x] auto-pair + `get_entitlement()` + `check_before_job()`
- [x] Ed25519 驗章（`_verify_entitlement_signature`）
- [x] 內建公鑰 + nonce 自動生成 + timestamp 自動填入
- [x] 離線政策判斷

### P11-4：Agent SDK 文件（Step 48）✅

- [x] Integration Guide v2.0
- [x] Onboarding Steps
- [x] Quick Connect Guide
- [x] Acceptance Test（6 關 19 項）
- [x] 對方 AI 驗收：19/19 全部通過

### P11-5：Server 部署上線（Step 49）✅

- [x] `deploy.php` 部署腳本（中文化，7 個操作按鈕）
- [x] Apache ProxyPass（`/api/` → `127.0.0.1:3100`）+ SPA fallback
- [x] Frontend build 修正（vue-tsc 與 Node 24 不相容，改用 `vite build`）
- [x] Backend PM2 路徑修正（`dist/main.js`）
- [x] CORS_ORIGIN 環境變數補齊
- [x] 部署至正式環境

### P11-5.5：Entitlement Notifications（Step 49.5）✅

- [x] Entitlement response 加入 `notifications` 陣列（signed payload 外、advisory only）
- [x] 4 類通知：GRACE_WARNING / EXPIRING_SOON / AGENT_UPDATE_REQUIRED / AGENT_UPDATE_AVAILABLE
- [x] Agent 版本控制：`AGENT_MIN_VERSION`（強制更新）/ `AGENT_LATEST_VERSION`（建議更新）
- [x] Semver 比較函數（compareVersions）
- [x] 更新 device.lastSeenAt + device.appVersion
- [x] GetEntitlementDto 新增 optional `agent_version`
- [x] AGENT_INTEGRATION_GUIDE.md 新增 §7.2 Notifications 文件
- [x] AGENT_ONBOARDING_STEPS.md 更新（entitlement body 加 agent_version）
- [x] Python 整合測試更新（驗證 notifications + key_id）
- [x] `nest build` 通過

### P11-6：Agent 正式對接測試（Step 50）🚧

- [ ] Admin 建立帳號 + License + 確認付款
- [ ] 產生 Pair Code 給 Agent 開發者
- [ ] Agent 配對成功（取得 device token）
- [ ] Entitlement 輪詢正常（Ed25519 驗章通過 + notifications 回傳）
- [ ] Pre-Job Check 正常
- [ ] 錯誤情境驗證（過期 / revoke / 指紋不符）
- [ ] Agent 版本通知驗證（AGENT_UPDATE_AVAILABLE / AGENT_UPDATE_REQUIRED）

---

## Phase 12：User 自助付款 ✅ 完成（2026-03-14）

### P12-1：User Checkout API（Step 51）✅

**後端：**
- [x] `POST /api/v1/payments/my/checkout` — User 端模擬付款
  - 驗證 License 屬於當前 User
  - 驗證 License status = ACTIVE
  - 驗證無 pending DRAFT Payment（→ PAYMENT_ALREADY_PENDING）
  - 驗證同一週期未重複 checkout（→ CHECKOUT_ALREADY_COMPLETED）
  - DataSource.transaction：建立 Payment CONFIRMED → Subscription activateOrRenew
  - 金額固定 990 TWD（MVP 單一方案）
  - audit log：PAYMENT_CHECKOUT_INITIATED + PAYMENT_CONFIRMED
  - notification：PAYMENT_CONFIRMED（交易外，non-blocking）
  - alert：PAYMENT_CONFIRMED（交易外）
- [x] DTO：UserCheckoutDto（license_id required）
- [x] 錯誤碼：LICENSE_NOT_FOUND / FORBIDDEN / LICENSE_NOT_ACTIVE / PAYMENT_ALREADY_PENDING / CHECKOUT_ALREADY_COMPLETED
- [x] AuditEvent 新增：PAYMENT_CHECKOUT_INITIATED

### P12-2：Portal Checkout UI（Step 52）✅

- [x] Portal `/portal/billing` 新增付款卡片（payableLicenses computed 篩選）
- [x] 付款確認 Dialog（方案名稱 + 金額 990 TWD + 序號 + 確認/取消按鈕）
- [x] 防重複送出（checkoutLoading flag）
- [x] 成功 toast + 自動刷新 licenses + payments
- [x] 5 種錯誤碼 i18n 對映
- [x] i18n：portal.billing.checkout.* + portal.billing.error.*

### P12-3：Checkout 整合測試（Step 53）✅ 8/8 場景通過

- [x] 首次付款 → Payment CONFIRMED + Subscription ACTIVE
- [x] ACTIVE 續費 → 新週期覆蓋
- [x] GRACE 續費 → 恢復 ACTIVE（邏輯共用 activateOrRenew）
- [x] SUSPENDED 續費 → 恢復 ACTIVE（同上）
- [x] 有 pending DRAFT → 409 PAYMENT_ALREADY_PENDING
- [x] 非自己的 License → 403 FORBIDDEN
- [x] License 未啟用 → 409 LICENSE_NOT_ACTIVE
- [x] 未登入 → 401

**決策**：ADR-024（User 自助模擬付款）
**BDD**：F51 + F52 + F53

---

## 技術決策摘要

| 項目 | 決策 |
|------|------|
| Backend | NestJS + TypeORM + TypeScript |
| Frontend | Vue 3 + Vite + Tailwind CSS + shadcn-vue（ADR-022） |
| Database | MySQL 8.0+（UTC 儲存，Asia/Taipei 商業計算）|
| Auth | JWT + Passport（User），Bearer device_token（Agent）|
| Signing | Ed25519（Entitlement response signing）|
| Scheduling | @nestjs/schedule（Subscription Reconciler）|
| Email | SendGrid |
| Port | Backend 3100 / Frontend 5174 |

---

## 相關文件

- `PROJECT_BASE.md` — 專案基礎與技術棧
- `STATE_RULES.md` — 狀態機唯一對照來源
- `API_CONTRACT_NOTES.md` — API 契約
- `DECISIONS.md` — 已拍板決策（ADR-011/012）
- `OPEN_QUESTIONS.md` — 待拍板問題（OQ-003 到 OQ-010）
- `DEV_SEQUENCE.md` — 開發順序總覽

---

## Phase 13：最小營運範疇 + 全自助訂購 ✅ 完成（2026-03-17）

> **目標**：從 MVP 基礎走向可實際營運的最小系統，補齊 Admin 操作效率 + User 基本體驗缺口
> **規格來源**：MVP 網站系統架構 v1.0 對照分析

### P13-A：補齊 Admin 現有頁面缺口

#### A1：Subscription 管理頁（Step 54）✅ F54 BDD 12/12
- [x] 後端：`GET /subscriptions/admin/list`（status + search + risk 篩選 + 分頁）
- [x] 後端：`GET /subscriptions/admin/:id`（詳情 + License/Account/Device/Payment 關聯摘要）
- [x] 前端：`/admin/subscriptions`（SubscriptionsView + SubscriptionDetailPanel）
- [x] 列表欄位：serialNo / accountEmail / status / periodEndDate / graceEndDate / updatedAt
- [x] 詳情面板：4 Tabs（基本資料 / 序號 / 設備 / 收款）
- [x] 到期風險篩選：`?risk=expiring_soon`（7天內 + GRACE + SUSPENDED）
- [x] 路由 + AdminLayout sidebar 加入

#### A2：付款紀錄篩選（Step 55）✅ F55 BDD 7/7
- [x] 後端：`adminFindAll` 移除預設 DRAFT，不帶 status 回傳全部
- [x] 後端：新增 `confirmedByEmail` batch load（join accounts by confirmedByAdminId）
- [x] 前端：`filterStatus` 預設改 `''`（全部）
- [x] 前端：Table 新增 confirmedAt + confirmedBy 兩欄

#### A3：License 詳情 → 建立付款單（Step 56）✅ F56 BDD 13/13
- [x] 後端：`adminCreateDraft` 新增 license 存在 + VOID 狀態檢查（422 LICENSE_VOIDED）
- [x] 前端：LicenseDetailPanel 新增「建立付款單」按鈕（VOID 不顯示）
- [x] 前端：Dialog 預填 licenseId（readonly）+ serialNo + 990 TWD + note
- [x] 前端：金額驗證（正整數）+ 防重複點擊 + 失敗保留 Dialog
- [x] 成功後 emit refresh → 重載 detail 顯示新 DRAFT

#### A4：訂閱狀態總覽（Step 57）✅ F57 BDD 8/8
- [x] DashboardView 新增「訂閱狀態總覽」區塊（三色進度條 + 3 張狀態卡 + 數量/百分比 + 總數）
- [x] KPI Cards 訂閱卡片跳轉改為 `/admin/subscriptions?status=XXX`
- [x] SubscriptionsView `onMounted` 讀取 `route.query.status` 自動帶入篩選
- [x] 後端 `GET /subscriptions/admin/stats`（ACTIVE/GRACE/SUSPENDED group by count）

### P13-B：新增 Admin 管理功能

#### B1：通知紀錄查看（Step 58）✅
- [x] 新增 `/admin/notifications` 頁面
- [x] 後端：`GET /notifications/admin/list`（支援 type / status 篩選 + 分頁）
- [x] 列表欄位：type / recipientEmail / status / sentAt / errorMessage / createdAt

#### B2：數據匯出（Step 59）✅ F59 BDD 8/8
- [x] 後端：3 支 CSV 匯出 API（`/licenses/admin/export` + `/payments/admin/export` + `/subscriptions/admin/export`）
- [x] 支援 `?status=` 篩選，無資料時回 header only
- [x] 共用工具：`common/utils/csv.ts`（toCsv + escape + BOM）
- [x] 前端：3 個管理頁各加「匯出 CSV」按鈕 + `downloadCsv` 工具函數
- [x] 每次匯出寫 `DATA_EXPORTED` audit log

### P13-C：User 功能

#### C1：忘記密碼（Step 60）✅ F60 BDD 11/11
- [x] Migration：`password_reset_tokens` 表（token_hash / status / expires_at / used_at）
- [x] 後端：`POST /auth/forgot-password`（不洩漏帳號存在性 / DISABLED 帳號不寄信 / 5次/15分鐘 rate limit）
- [x] 後端：`POST /auth/reset-password`（SHA-256 hash 驗證 / 30 分鐘 TTL / DB 條件更新防併發）
- [x] Email：PASSWORD_RESET 模板（重設連結按鈕 + 過期時間提示）
- [x] 前端：ForgotPasswordView + ResetPasswordView + 登入頁「忘記密碼？」連結
- [x] Audit：PASSWORD_RESET_REQUESTED / COMPLETED / REJECTED

### P13-D：方案與訂購

#### D1：方案管理模組（Step 61）✅ F61 BDD 13/13，ADR-025
- [x] Migration：`plans` 表 + `compare_features` 表 + `licenses` 新增 `plan_id` / `snapshot_price`
- [x] Seed：3 個預設方案（Basic 990 / Pro 1990 / Business 4990）
- [x] 後端：Plan Admin CRUD + 公開方案 API + CompareFeature CRUD
- [x] 後端：License 建立時指定 planId + `adminAssignPlan` API（快照價）
- [x] 後端：Checkout 從 `license.snapshotPrice` 讀金額
- [x] 前端：`/admin/plans` 管理頁（列表 + PlanEditView）
- [x] 前端：Landing `/pricing` 100% 從 API 動態讀取
- [x] 權限：`plan.read` + `plan.write`

#### D2：全自助訂購流程 v2（Step 62）✅ F62 v2 BDD 43 場景，ADR-026 + ADR-027
- [x] 後端：`POST /api/v1/checkout/subscribe`（原子操作：Plan → License → Payment → Subscription）
- [x] 自動生成 serialNo（`MUC-YYYYMM-XXXXX`）
- [x] 前端：`/portal/checkout?plan=XXX` 確認頁 + 成功頁
- [x] 前端：`/pricing` CTA 按鈕帶 plan code 跳轉（未登入先登入再跳）
- [x] 商業規則：一個帳號可訂多個 License
- [x] Admin 手動建 License 流程保留

---

## Phase 14：Credit 點數系統 + 產品優化 🚧

> **目標**：建立 Credit 計量系統，支撐按用量計費
> **規劃文件**：memory/credit-system-design.md / memory/phase14-planning.md
> **BDD**：F67（26 個場景）

### P0：Credit 點數系統 ✅ 完成（2026-03-22）
- [x] Migration：`credit_balances` / `credit_transactions` 表（F67）
- [x] Credit consume API（`POST /api/v1/credits/consume`）— idempotency + FOR UPDATE 鎖 + 餘額檢查
- [x] Credit balance API（`GET /api/v1/credits/licenses/:id/balance`）
- [x] Credit transactions API（`GET /api/v1/credits/licenses/:id/transactions`）— 分頁
- [x] Admin balance API（`GET /api/v1/admin/credits/licenses/:id/balance`）
- [x] Checkout / Payment confirm 初始化 credit_balance = plan.creditsPerMonth
- [x] Portal `/portal/credits`（餘額卡片 + 進度條 + 交易紀錄）
- [x] Admin License 詳情加「點數」tab
- [x] Audit: CREDIT_CONSUMED / CREDIT_CONSUME_FAILED / CREDIT_GRANTED
- [x] API 測試 15/15 場景通過

### P0.5：Portal 續約流程改善 ✅ 完成（2026-03-22）
- [x] Portal /pricing 分區顯示（續約 + 首購）
- [x] ShopCheckoutView 支援 RENEWAL 方案 + License 選擇器
- [x] LicenseView 續約連結帶 licenseId
- [x] 重構：ShopCheckoutView 拆出 PaymentMethodSelector + LicenseSelector
- [x] 重構：OrdersView 拆出 OrderDetailPanel

### P1：方案月配點數（待開工）
- [ ] 方案月配點數設定（plans.creditsPerMonth 已有欄位）
- [ ] 週期開始自動配點（Reconciler 整合，type=GRANT）
- [ ] 超額計費（USD 0.1/點，方案設定超額上限）
- [ ] 超額帳單產生（獨立於訂閱帳單）
- [ ] Agent SDK 整合（consume + balance）

### P1：Agent 下載機制
- [ ] Agent 安裝包管理（版本 + 平台）
- [ ] 下載頁面 / 連結產生
- [ ] 版本更新通知整合（已有 AGENT_UPDATE_AVAILABLE 基礎）

### P2：同設備多 License（評估中）
- [ ] 評估一台設備綁多個 License 的影響範圍
- [ ] Domain Model 調整方案

### P3：SDK 安全強化
- [ ] Token 加密儲存強化
- [ ] Anti-replay 進階策略

---

## SEO / GEO 成長模組 ✅ 完成（2026-03-22）

> **定位**：獨立成長模組，不影響授權、訂購、收款主流程
> **BDD**：F71（20 個場景，分兩批）

### 第一批：CRUD + 發布 + Sitemap + GEO + 權限（S01-S16）✅
- [x] Migration：seo_pages 表（SEO + GEO + Metrics 共 ~25 欄位）
- [x] SeoPage Service + Controller（CRUD + 發布 + 封存 + sitemap）10 支 API
- [x] 驗證規則：slug 唯一、title 必填、metaDescription 長度、schema JSON 合法性
- [x] canonicalUrl 自動生成 + robotsPolicy → sitemap 自動同步
- [x] sitemap.xml 動態輸出 + GEO 欄位管理
- [x] 權限：seo.read / seo.create / seo.update / seo.publish / seo.metrics.read
- [x] Admin 列表頁 + 編輯頁（4 tab：SEO/GEO/Schema/成效）
- [x] SchemaEditor 視覺化元件（FAQ Q&A 編輯器 + 一般頁面表單 + 原始 JSON）
- [x] 7 個 Audit event + 5 筆 seed data
- [x] API 測試 16/16 通過

### 第二批：預覽 + 成效指標 + FAQ 同步（S17-S20）✅
- [x] 頁面預覽 API
- [x] 成效指標同步 API + 查詢 UI
- [x] FAQ 內容同步自動更新 FAQPage schema + GEO answerSnippet
- [x] API 測試 4/4 通過

### SEO 基礎建設 ✅
- [x] index.html：完整 meta + OG + Twitter Card + 3 組 JSON-LD + noscript
- [x] robots.txt + sitemap 指向
- [x] useSeoMeta composable（動態 meta/title/OG/JSON-LD）
- [x] inject-seo.cjs：build 後注入靜態 HTML（首頁 + 定價頁）
- [x] npm run build 整合 SEO 注入

---

## Phase 15：自動金流串接 ⬜

> **前置條件**：選定金流商後開工
> **定位**：串接真實付款（信用卡/ATM/轉帳），取代人工確認
> **架構基礎**：F66 已完成 PaymentMethod 管理 + 結帳流程分支（ADR-030）
> **預留接口**：checkout.service 的 isInstantPayment 分支 → 改為 hasGateway

- [x] PaymentMethod 管理（Admin CRUD + 結帳選擇）— F66 已完成
- [x] 非即時付款流程（ATM/匯款 → DRAFT → Admin 確認）— F66 已完成
- [ ] 金流服務商選定（台灣 Pay？綠界？藍新？）
- [ ] Gateway Service 模組（建立交易 → 取得付款 URL）
- [ ] Webhook Controller（`POST /payment/gateway-callback`）
- [ ] Webhook 驗簽 + Payment CONFIRMED + Subscription ACTIVE
- [ ] 前端：收到 paymentUrl → redirect 到金流頁
- [ ] 前端：`/shop/payment-return`（金流回調 landing page）
- [ ] 退款 API + 流程
- [ ] 發票服務串接（電子發票）
- [ ] Admin 金流紀錄頁

---

## Phase 16：推薦分潤 + 介紹人折抵 ⬜

> **定位**：推薦獎勵機制，帶動口碑獲客

- [ ] Referral 模組設計（referral_code / referral_records 表）
- [ ] Account 新增 referral_code 欄位
- [ ] 推薦連結產生 + 追蹤
- [ ] 推薦成功判定邏輯（被推薦者首次付費 = 成功）
- [ ] 推薦獎勵發放（送 1 個月額外使用額度 — 具體定義待確認）
- [ ] 推薦紀錄查詢（User 端 + Admin 端）
- [ ] 介紹人折抵機制（首次付款折扣 or 點數贈送）
- [ ] Portal 推薦頁面（我的推薦碼 + 推薦紀錄）
- [ ] Admin 推薦報表

---

## Phase 17：自動更新發佈平台 ⬜

> **定位**：Agent 子機自動更新機制，取代手動部署

- [ ] 版本管理模組（version / platform / release_notes / download_url）
- [ ] Agent 安裝包上傳 + 儲存（files 模組整合）
- [ ] 版本發佈 API（Admin 端）
- [ ] Agent 端版本檢查 + 自動下載（已有 AGENT_MIN_VERSION / AGENT_LATEST_VERSION 基礎）
- [ ] 強制更新 vs 建議更新邏輯
- [ ] 更新進度回報
- [ ] Admin 版本管理頁（發佈 / 歷史版本 / 裝機率統計）
- [ ] 灰度發佈（按比例推送）

---

📅 **建立日期**：2026-03-10
📌 **目前進度**：Phase 1-13 全部完成（Step 1-62），Phase 14-17 規劃中
🔗 **下一步**：Phase 14 P0 Credit 點數系統 → Phase 15 自動金流（待付款方案確認）
